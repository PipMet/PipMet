% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize_data.r
\name{normalize_data}
\alias{normalize_data}
\title{Normalize data}
\usage{
normalize_data(
  anIC,
  pslist,
  metadata,
  myDir,
  pre_anno,
  pic_extension = c(".tiff", ".png"),
  derivatization = NULL,
  mergeCompounds = NULL,
  removeCompounds = NULL,
  group = NULL
)
}
\arguments{
\item{anIC}{A 'xsAnnotate' CAMERA object with grouped spectra.}

\item{pslist}{List of spectra.}

\item{metadata}{A matrix or data.frame with metadata information about samples. Include, at least 'sample' and 'file' columns with name of sample and its path, respectively. More information can be added in new columns, such as 'group', 'class', 'biorep' and 'tecrep'.}

\item{myDir}{Path to the directory of work.}

\item{pre_anno}{A table with annotations for spectra. or path to .csv file.}

\item{pic_extension}{Character. Pictures format to generate. Supported = '.tiff', '.png'. Default to c('.tiff', '.png').}

\item{derivatization}{Character. Kind of derivatization the samples were prepared with. Supported are 'Trimethylsilyl' and 'None'. If NULL, the user will be asked. Default to 'NULL'.}

\item{mergeCompounds}{Logical. If TRUE, the user may choose from a pop-up what compounds identified should be representated as one with intensities summed. Exemple: derivatizations derivatives. If NULL, the user will be asked. Default to 'NULL'.}

\item{removeCompounds}{Logical. If TRUE, the user may choose from a pop-up identified compounds to remove from the quantification table. If NULL, the user will be asked. Default to 'NULL'.}

\item{group}{Character. Name of metadata column to group the samples for normalization. Default to NULL.}
}
\value{
A matrix of all spectra, with their annotation (if available), most intense peak m/z and its intensities in every sample.
}
\description{
This function normalize data.
}
\examples{
\donttest{
\dontrun{
pre_anno <- read.csv(system.file('extdata', 'pre_anno.csv', package = 'PipMet'))
load(system.file('extdata', 'pslist.RData', package = 'PipMet'))
load(system.file('extdata', 'anIC.RData', package = 'PipMet'))
load(system.file('extdata', 'metadata.RData', package = 'PipMet'))
normalized <- normalize_data(
  anIC,
  pslist,
  metadata,
  myDir = '~/',
  pre_anno = system.file('extdata', 'pre_anno.csv', package = 'PipMet'),
  derivatization = 'Trimethylsilyl',
  mergeCompounds = FALSE,
  removeCompounds = FALSE,
  group = 'group'
)
}
}
}
